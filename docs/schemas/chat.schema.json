{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ChatConfig",
  "description": "quiver-chat configuration. Verbose on purpose: this file IS the\ntool's interface. The future Quiver UI generates files of this shape\nfrom the JSON Schema exported via `quiver-chat --print-schema`.",
  "type": "object",
  "properties": {
    "server": {
      "description": "HTTP server and widget frontend location.",
      "$ref": "#/$defs/ServerConfig"
    },
    "twitch": {
      "description": "Twitch chat source and optional API credentials.",
      "$ref": "#/$defs/TwitchConfig"
    },
    "theme": {
      "description": "Visual appearance of rendered messages.",
      "$ref": "#/$defs/ThemeConfig"
    },
    "emotes": {
      "description": "Emote source toggles. All on by default; disabling a provider makes\nits emojis vanish from rendered messages entirely.",
      "$ref": "#/$defs/EmotesConfig",
      "default": {
        "twitch": true,
        "unicode": true,
        "seventv": true,
        "bttv": true,
        "ffz": true
      }
    },
    "filters": {
      "description": "Message filtering (allowlist/denylist per dimension). Absent = no\nfiltering at all.",
      "$ref": "#/$defs/FiltersConfig",
      "default": {
        "display_name": null,
        "username": null,
        "user_id": null,
        "content": null,
        "message_type": null,
        "role": null
      }
    }
  },
  "required": [
    "server",
    "twitch",
    "theme"
  ],
  "$defs": {
    "ServerConfig": {
      "type": "object",
      "properties": {
        "listen": {
          "description": "`host:port` the widget HTTP+WS server binds to.",
          "type": "string"
        },
        "widget_dist": {
          "description": "Optional absolute path to the built widget frontend directory.\nUnset → placeholder page is served at `/`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "listen"
      ]
    },
    "TwitchConfig": {
      "type": "object",
      "properties": {
        "channel": {
          "description": "Twitch channel login to read chat from (read-only anonymous join).",
          "type": "string"
        },
        "client_id": {
          "description": "Twitch API client id — enables badge image URLs (Helix lookup).",
          "type": [
            "string",
            "null"
          ]
        },
        "client_secret": {
          "description": "Twitch API client secret. Keep ONLY in git-ignored local configs.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "channel"
      ]
    },
    "ThemeConfig": {
      "type": "object",
      "properties": {
        "font_size_px": {
          "description": "Base font size for messages, in CSS pixels.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "max_messages": {
          "description": "Maximum number of messages kept in the visible history.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "message_lifetime_secs": {
          "description": "Seconds a message stays visible before fading out.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "custom_css": {
          "description": "Your own CSS, applied after the built-in widget styles. Authored as\na RON raw string so newlines and quotes stay untouched.\nParsed with lightningcss at load/reload; a parse error is reported\nas a warning but never blocks the tool.",
          "type": [
            "string",
            "null"
          ]
        },
        "role_css": {
          "description": "Per-role CSS: Twitch badge set id -> CSS snippet applied to messages\nwhose sender carries that badge. Message rows get `role-<id>` classes,\nsnippets are injected before custom_css. Common ids: broadcaster,\nmoderator, vip, subscriber, founder.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        }
      },
      "required": [
        "font_size_px",
        "max_messages",
        "message_lifetime_secs"
      ]
    },
    "EmotesConfig": {
      "description": "Per-provider emote switches, all enabled by default.",
      "type": "object",
      "properties": {
        "twitch": {
          "description": "First-party Twitch emotes (rendered from per-message ranges).",
          "type": "boolean",
          "default": true
        },
        "unicode": {
          "description": "Native unicode emoji characters. `false` strips them from text.",
          "type": "boolean",
          "default": true
        },
        "seventv": {
          "description": "7TV global + channel sets.",
          "type": "boolean",
          "default": true
        },
        "bttv": {
          "description": "BetterTTV global + channel/shared sets.",
          "type": "boolean",
          "default": true
        },
        "ffz": {
          "description": "FrankerFaceZ global + room sets.",
          "type": "boolean",
          "default": true
        }
      }
    },
    "FiltersConfig": {
      "description": "Message filtering rules. Dimensions combine with AND: a message is\nrendered only when EVERY active dimension passes.",
      "type": "object",
      "properties": {
        "display_name": {
          "description": "Regex matched against the sender's display name.",
          "anyOf": [
            {
              "$ref": "#/$defs/ListFilter"
            },
            {
              "type": "null"
            }
          ]
        },
        "username": {
          "description": "Regex matched against the sender's login name (always lowercase).",
          "anyOf": [
            {
              "$ref": "#/$defs/ListFilter"
            },
            {
              "type": "null"
            }
          ]
        },
        "user_id": {
          "description": "EXACT match against the sender's Twitch user id (numeric string).",
          "anyOf": [
            {
              "$ref": "#/$defs/ListFilter"
            },
            {
              "type": "null"
            }
          ]
        },
        "content": {
          "description": "Regex matched against the message text. Events have empty text.\nUse patterns like `^!` to filter chat commands.",
          "anyOf": [
            {
              "$ref": "#/$defs/ListFilter"
            },
            {
              "type": "null"
            }
          ]
        },
        "message_type": {
          "description": "Message kind names: message | sub | gift_sub | mystery_gift |\nraid. Unknown names are config errors.",
          "anyOf": [
            {
              "$ref": "#/$defs/ListFilter"
            },
            {
              "type": "null"
            }
          ]
        },
        "role": {
          "description": "Badge ids carried by the sender — same vocabulary as role_css\n(moderator, vip, subscriber, ...). Events carry no badges in v1.",
          "anyOf": [
            {
              "$ref": "#/$defs/ListFilter"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "ListFilter": {
      "description": "One filter dimension: a direction plus its string items.\nItem interpretation depends on the dimension (regex / exact id /\nkind name / badge id) — see `FiltersConfig` field docs.",
      "type": "object",
      "properties": {
        "mode": {
          "$ref": "#/$defs/Mode"
        },
        "items": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "mode",
        "items"
      ]
    },
    "Mode": {
      "description": "Filter list direction. Allowlist = at least one item must match;\ndenylist = no item may match. An EMPTY item list makes the whole\ndimension inactive (an empty allowlist meaning \"drop everything\"\nis a footgun we refuse to arm).",
      "type": "string",
      "enum": [
        "allowlist",
        "denylist"
      ]
    }
  }
}
