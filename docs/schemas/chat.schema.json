{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ChatConfig",
  "type": "object",
  "properties": {
    "server": {
      "$ref": "#/$defs/ServerConfig"
    },
    "theme": {
      "$ref": "#/$defs/ThemeConfig"
    },
    "twitch": {
      "$ref": "#/$defs/TwitchConfig"
    }
  },
  "required": [
    "server",
    "twitch",
    "theme"
  ],
  "$defs": {
    "ServerConfig": {
      "type": "object",
      "properties": {
        "listen": {
          "description": "`host:port` the widget HTTP+WS server binds to.",
          "type": "string"
        },
        "widget_dist": {
          "description": "Optional absolute path to the built widget frontend directory.\nUnset → placeholder page is served at `/`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "listen"
      ]
    },
    "ThemeConfig": {
      "type": "object",
      "properties": {
        "custom_css": {
          "description": "Your own CSS, applied after the built-in widget styles. Authored as\na RON raw string so newlines and quotes stay untouched.\nParsed with lightningcss at load/reload; a parse error is reported\nas a warning but never blocks the tool.",
          "type": [
            "string",
            "null"
          ]
        },
        "font_size_px": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "max_messages": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "message_lifetime_secs": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "role_css": {
          "description": "Per-role CSS: Twitch badge set id -> CSS snippet applied to messages\nwhose sender carries that badge. Message rows get `role-<id>` classes,\nsnippets are injected before custom_css. Common ids: broadcaster,\nmoderator, vip, subscriber, founder.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        }
      },
      "required": [
        "font_size_px",
        "max_messages",
        "message_lifetime_secs"
      ]
    },
    "TwitchConfig": {
      "type": "object",
      "properties": {
        "channel": {
          "description": "Twitch channel login to read chat from (read-only anonymous join).",
          "type": "string"
        },
        "client_id": {
          "description": "Twitch API client id — enables badge image URLs (Helix lookup).",
          "type": [
            "string",
            "null"
          ]
        },
        "client_secret": {
          "description": "Twitch API client secret. Keep ONLY in git-ignored local configs.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "channel"
      ]
    }
  }
}
